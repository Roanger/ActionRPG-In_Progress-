#pragma once

// Common vertex structure for rendering
struct Vertex {
    float position[2];  // 2D position (x, y)
    float color[3];     // RGB color
    float texCoord[2];  // 2D texture coordinates (u, v)
};
